@page
@model UNA.PraticasProgramacao.Web.Pages.UnidMedida.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout_Grid.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UnidadeMedida[0].CodUnidadeMedida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UnidadeMedida[0].NomeUnidadeMedida)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.UnidadeMedida) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CodUnidadeMedida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomeUnidadeMedida)
            </td>
            <td>
                <a class="btn btn-primary btn-circle btn-sm" asp-page="./Edit" asp-route-id="@item.IdUnidadeMedida">Edit</a> |
                <a class="btn btn-primary btn-circle btn-sm" asp-page="./Details" asp-route-id="@item.IdUnidadeMedida">Details</a> |
                <a class="btn btn-primary btn-circle btn-sm" asp-page="./Delete" asp-route-id="@item.IdUnidadeMedida">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
